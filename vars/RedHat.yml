---
# role: ansible-role-acme_netcup
# file: vars/RedHat.yml

acme_netcup_packages:
  - openssl
  - python3-cryptography
  - python3-dns
  - python3-pip

acme_netcup_pip_extra_args: "--user --upgrade"
